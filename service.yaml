apiVersion: v1
kind: Service
metadata:
  name: summersafe
  labels:
    app: summersafe
spec:
  selector:
    app: summersafe
  ports:
  - name: http
    port: 80
    targetPort: 8000
  type: ClusterIP